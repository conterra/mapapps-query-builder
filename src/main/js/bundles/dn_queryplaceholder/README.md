# dn_queryplaceholder

Replaces placeholder in the QueryBuilder bundle.

## Usage

Add your own replacer strings.

## Configuration Reference

### Replacer:
```json
"Replacer": {
    "placeholder": {
        "user": "Müller"
    }
}
```
